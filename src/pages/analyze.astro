---
// This page requires auth — the AnalysisAgent component handles it client-side.
import { AnalysisAgent } from '../components/AnalysisAgent.tsx';
---
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width" />
  <title>Analyze — unnatural</title>
  <style>
    * { box-sizing: border-box; margin: 0; padding: 0; }
    body {
      font-family: system-ui, sans-serif;
      max-width: 800px;
      margin: 0 auto;
      padding: 2rem 1rem;
      color: #111;
    }
    p { margin-bottom: 0.5rem; }
    h1, h2, h3, h4, h5, h6 { margin: 1rem 0 0.5rem; line-height: 1.25; }
    li { margin-left: 1.25rem; }
    code {
      font-family: ui-monospace, monospace;
      background: #f2f2f2;
      padding: 0.1em 0.3em;
      border-radius: 3px;
      font-size: 0.92em;
    }
  </style>
</head>
<body>
  <!-- React island, client-side only -->
  <AnalysisAgent client:only="react" />
</body>
</html>
