---
// This page requires auth — the AnalysisAgent component handles it client-side.
import { AnalysisAgent } from '../components/AnalysisAgent.tsx';
import BaseLayout from '../layouts/BaseLayout.astro';
import SiteHeader from '../components/SiteHeader.astro';
import SiteFooter from '../components/SiteFooter.astro';
import '../styles/book.css';
---
<BaseLayout title="Analyze" description="Explore a question with the book's framework." cinematic>
  <SiteHeader />
  <main class="book-page" id="main-content"><AnalysisAgent client:only="react" /></main>
  <SiteFooter />
</BaseLayout>
